runtime: python
env: flex

runtime_config:
  python_version: "3.12"

entrypoint: gunicorn --bind :$PORT app.app:app

handlers:
- url: /
  script: auto

automatic_scaling:
  min_num_instances: 1
  max_num_instances: 2
